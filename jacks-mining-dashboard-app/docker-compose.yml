version: "3.7"

services:

  app_proxy:
    environment:
      # The format here is: <app-id>_<docker-service-name>_1
      APP_HOST: jacks-mining-dashboard-app_web_1
      APP_PORT: 3000
    
  server:
    image: j1441/mining-dashboard-app:latest
    user: "1000:1000"
    init: true
